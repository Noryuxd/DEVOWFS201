import React from "react";
import { axiosClient } from "../api/axios";

// Use async arrowed function to make API calls
// const axios = await axiosClient.post('/login', data)

const Login = () => {
  return <div>Login</div>;
};

export default Login;
